eureka:
  client:
    service-url:
      default-zone: http://EUREKA-SERVER
      
spring:   
  cloud:
    gateway:
      routes:
      - id: studentService
        uri:  lb://STUDENT-SERVICE
        predicates:
        - Path=/** 
